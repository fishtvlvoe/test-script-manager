---
phase: 03-monaco-editor-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - includes/admin/class-admin-page.php
  - assets/js/monaco-loader.js
  - assets/js/admin-page.js
  - assets/css/admin-page.css
autonomous: true

must_haves:
  truths:
    - "Monaco Editor loads on the script edit page"
    - "PHP code displays with syntax highlighting"
    - "Editor container has proper dimensions and auto-resizes"
    - "User can type and edit code in the Monaco editor"
  artifacts:
    - path: "assets/js/monaco-loader.js"
      provides: "Monaco CDN initialization and editor creation"
      min_lines: 80
    - path: "includes/admin/class-admin-page.php"
      provides: "Monaco script enqueuing"
      contains: "monaco-loader"
  key_links:
    - from: "includes/admin/class-admin-page.php"
      to: "assets/js/monaco-loader.js"
      via: "wp_enqueue_script with monaco-loader dependency"
      pattern: "wp_enqueue_script.*monaco-loader"
    - from: "assets/js/monaco-loader.js"
      to: "jsDelivr CDN"
      via: "require.config paths"
      pattern: "cdn\\.jsdelivr\\.net.*monaco-editor"
---

<objective>
Integrate Monaco Editor via CDN loading with basic PHP syntax highlighting support.

Purpose: Replace the plain textarea with a professional-grade code editor that developers expect (VS Code quality).
Output: Working Monaco editor on the script edit page with PHP syntax highlighting.
</objective>

<execution_context>
@/Users/fishtv/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fishtv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-monaco-editor-integration/03-RESEARCH.md

# Existing files to modify
@includes/admin/class-admin-page.php
@assets/js/admin-page.js
@assets/css/admin-page.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Monaco loader and enqueue scripts</name>
  <files>
    - assets/js/monaco-loader.js
    - includes/admin/class-admin-page.php
  </files>
  <action>
Create `assets/js/monaco-loader.js` that:
1. Configures Monaco CDN path using `require.config({ paths: { 'vs': 'https://cdn.jsdelivr.net/npm/monaco-editor@0.55.1/min/vs' } })`
2. Loads Monaco via `require(['vs/editor/editor.main'], function() {...})`
3. Exposes initialization function `window.tsmInitMonaco(containerId, options)` for flexible editor creation
4. Handles loading state with a flag to prevent double initialization

Update `includes/admin/class-admin-page.php` enqueue_assets() method:
1. Add Monaco loader script from CDN: `https://cdn.jsdelivr.net/npm/monaco-editor@0.55.1/min/vs/loader.js`
2. Enqueue `assets/js/monaco-loader.js` with monaco-loader as dependency
3. Add `wp_localize_script` for `tsmMonaco` object with:
   - `cdnPath`: CDN base path for Monaco
   - `ajaxUrl`: admin_url('admin-ajax.php')
   - `nonce`: wp_create_nonce('tsm_editor')
   - `defaultTheme`: 'vs-dark'
4. Keep existing admin-page.js enqueue (will use monaco-loader)

Use Monaco version 0.55.1 consistently (from research).
  </action>
  <verify>
1. `grep -r "monaco-loader" includes/admin/class-admin-page.php` shows enqueue
2. File `assets/js/monaco-loader.js` exists and contains `cdn.jsdelivr.net`
3. `grep "wp_localize_script" includes/admin/class-admin-page.php` shows tsmMonaco data
  </verify>
  <done>
Monaco loader script enqueued on admin page, CDN path configured, tsmMonaco data available to JavaScript.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate Monaco editor into admin page UI</name>
  <files>
    - assets/js/admin-page.js
    - assets/css/admin-page.css
    - includes/admin/class-admin-page.php
  </files>
  <action>
Update `includes/admin/class-admin-page.php` render_page():
1. Replace the textarea in the create form with a div: `<div id="monaco-editor-create" class="tsm-monaco-editor"></div>`
2. Add hidden input to store editor value: `<input type="hidden" id="tsm-script-code-input" name="code" />`
3. Add edit mode section with Monaco container: `<div id="tsm-edit-section">` containing `<div id="monaco-editor-edit" class="tsm-monaco-editor"></div>`
4. Ensure PHP opening tag `<?php` is mentioned in placeholder/instruction text

Update `assets/css/admin-page.css`:
1. Add `.tsm-monaco-editor { height: 400px; border: 1px solid #ddd; }` for explicit height (critical per research)
2. Add responsive styles for smaller screens

Update `assets/js/admin-page.js`:
1. Wait for Monaco to load: Check `window.monaco` or use callback from monaco-loader
2. Create editor instance when showing create/edit form:
   ```javascript
   window.tsmCreateEditor = monaco.editor.create(document.getElementById('monaco-editor-create'), {
     value: '<?php\n\n// Your test script here\n',
     language: 'php',
     theme: tsmMonaco.defaultTheme || 'vs-dark',
     automaticLayout: true,
     minimap: { enabled: false },
     fontSize: 14,
     wordWrap: 'on',
     scrollBeyondLastLine: false
   });
   ```
3. Sync editor value to hidden input before form submission
4. Handle editor for edit mode (load existing script content)

IMPORTANT:
- Do NOT remove existing textarea handling until Monaco is fully working
- Use `automaticLayout: true` to handle container resizing
- Always include `<?php` tag in editor default value (per research pitfall)
  </action>
  <verify>
1. Visit https://test.buygo.me/wp-admin/admin.php?page=test-script-manager
2. Click "新增腳本" button
3. Monaco editor should appear with PHP syntax highlighting
4. PHP keywords like `echo`, `function`, `if` should be colored
5. Browser console should not show Monaco-related errors
  </verify>
  <done>
Monaco editor renders in create form with PHP syntax highlighting, proper height, and dark theme.
Editor value syncs to hidden input for form submission.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add script editing with Monaco</name>
  <files>
    - assets/js/admin-page.js
    - includes/admin/class-admin-page.php
  </files>
  <action>
Update `includes/admin/class-admin-page.php`:
1. Add edit view section in HTML that shows when clicking a script in the list
2. Include Monaco container `<div id="monaco-editor-edit" class="tsm-monaco-editor"></div>`
3. Add script name display, save button, and execute button placeholders
4. Pass script data via `wp_localize_script` when editing (or use data attributes)

Update `assets/js/admin-page.js`:
1. Add click handler for script list items to enter edit mode
2. Fetch script content via REST API: `GET /wp-json/test-script-manager/v1/scripts/{id}`
3. Create Monaco editor for edit view (destroy previous instance first if exists)
4. Set editor value from API response:
   ```javascript
   editor.setValue(script.code);
   ```
5. Use `isLoading = true` flag before setValue to prevent auto-save trigger (per research pitfall)
6. Add "Back to list" button handler to switch views

Ensure:
- Only one Monaco editor instance active at a time (dispose old before creating new)
- Editor content loaded from database, not filesystem
- Handle 404 errors gracefully (script deleted by another user)
  </action>
  <verify>
1. Create a test script via UI
2. Click on the script in the list
3. Edit view should appear with Monaco editor
4. Editor should contain the script's actual code from database
5. PHP syntax highlighting should work in edit mode
  </verify>
  <done>
Users can click on a script to open edit view with Monaco editor.
Script content loads from REST API with syntax highlighting.
Edit and create modes use separate Monaco instances properly disposed.
  </done>
</task>

</tasks>

<verification>
After all tasks:

1. **Create mode works:**
   - Click "新增腳本" shows Monaco editor
   - PHP highlighting active (colored keywords)
   - Can type code and submit form

2. **Edit mode works:**
   - Click existing script shows Monaco editor with script content
   - Correct code loaded from database
   - PHP highlighting active

3. **No JavaScript errors:**
   - Browser console clean of Monaco-related errors
   - No "require is not defined" errors (AMD loader working)

4. **Proper layout:**
   - Editor has visible height (not 0px)
   - Editor resizes with window (automaticLayout)
</verification>

<success_criteria>
- Monaco Editor loads via CDN without errors
- PHP syntax highlighting visible (keywords colored)
- Create form uses Monaco editor instead of textarea
- Edit view shows script content in Monaco editor
- Editor height is 400px, properly styled
- Browser console has no Monaco-related errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-monaco-editor-integration/03-01-SUMMARY.md`
</output>
