---
phase: 07-output-enhancements-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - includes/services/class-settings-service.php
  - includes/admin/class-settings-page.php
  - includes/services/class-execution-service.php
  - includes/class-plugin.php
autonomous: true

must_haves:
  truths:
    - "User can access Settings submenu under Test Script Manager"
    - "User can configure execution timeout (1-300 seconds)"
    - "User can enable/disable IP whitelist"
    - "User can add IP addresses to whitelist (one per line)"
    - "Script execution is blocked if IP is not whitelisted (when enabled)"
    - "Settings persist after saving and page reload"
    - "Configured timeout is used during script execution"
  artifacts:
    - path: "includes/services/class-settings-service.php"
      provides: "Settings get/update operations and IP validation"
      exports: ["SettingsService"]
      min_lines: 80
    - path: "includes/admin/class-settings-page.php"
      provides: "Settings page UI with General and Security tabs"
      exports: ["Settings_Page"]
      min_lines: 100
  key_links:
    - from: "includes/services/class-execution-service.php"
      to: "includes/services/class-settings-service.php"
      via: "SettingsService::is_ip_whitelisted() check before execution"
      pattern: "is_ip_whitelisted"
    - from: "includes/services/class-execution-service.php"
      to: "includes/services/class-settings-service.php"
      via: "SettingsService::get_timeout() for execution timeout"
      pattern: "SettingsService::get_timeout"
    - from: "includes/admin/class-settings-page.php"
      to: "WordPress Settings API"
      via: "register_setting(), add_settings_section(), add_settings_field()"
      pattern: "register_setting"
---

<objective>
Create a Settings page with WordPress Settings API for configuring execution timeout and IP whitelist security controls.

Purpose: Administrators need to configure plugin behavior without editing code. The settings page provides a user-friendly interface for security controls (IP whitelist) and execution parameters (timeout).

Output: SettingsService for option management, Settings_Page with tabbed UI (General, Security), and IP validation integrated into ExecutionService.
</objective>

<execution_context>
@/Users/fishtv/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fishtv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-output-enhancements-polish/07-RESEARCH.md

# Relevant source files
@includes/services/class-execution-service.php
@includes/admin/class-admin-page.php
@includes/class-plugin.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SettingsService with IP validation</name>
  <files>includes/services/class-settings-service.php</files>
  <action>
Create SettingsService class in TSM\Services namespace:

1. **Option constants**:
   - const OPTION_TIMEOUT = 'tsm_execution_timeout'
   - const OPTION_OUTPUT_LIMIT = 'tsm_output_limit'
   - const OPTION_IP_WHITELIST_ENABLED = 'tsm_ip_whitelist_enabled'
   - const OPTION_IP_WHITELIST = 'tsm_ip_whitelist'

2. **get_timeout()** static method:
   - Return get_option(self::OPTION_TIMEOUT, 30)
   - Cast to int, clamp to 1-300 range

3. **get_output_limit()** static method:
   - Return get_option(self::OPTION_OUTPUT_LIMIT, 10485760) // 10MB default
   - Cast to int

4. **is_ip_whitelist_enabled()** static method:
   - Return (bool) get_option(self::OPTION_IP_WHITELIST_ENABLED, false)

5. **get_ip_whitelist()** static method:
   - Return get_option(self::OPTION_IP_WHITELIST, '')
   - Returns newline-separated string of IPs

6. **is_ip_whitelisted($ip = null)** static method:
   - If $ip is null, use $_SERVER['REMOTE_ADDR']
   - If whitelist not enabled, return true (allow all)
   - Parse whitelist into array (explode by \n, trim each, filter empty)
   - If whitelist empty, return true (allow all)
   - Check exact IP match with in_array()
   - Check CIDR ranges using ip_in_range() helper
   - Return true if matched, false otherwise

7. **ip_in_range($ip, $range)** private static method:
   - If range doesn't contain '/', return false
   - Extract subnet and bits from CIDR notation
   - Use ip2long() for both IP and subnet
   - Calculate mask: -1 << (32 - $bits)
   - Apply mask to both and compare
   - Return true if match

8. **sanitize_ip_whitelist($value)** static method (for Settings API callback):
   - Explode by \n
   - Trim each line
   - Filter to only valid IPs or CIDRs
   - Use filter_var($line, FILTER_VALIDATE_IP) or is_valid_cidr()
   - Return implode("\n", $valid_ips)

9. **is_valid_cidr($cidr)** private static method:
   - Check if contains '/'
   - Split into IP and bits
   - Validate IP with filter_var()
   - Validate bits is numeric and 0-32 range
   - Return boolean

Include proper namespace, class docblock, PHPDoc for all methods.
  </action>
  <verify>
File exists at includes/services/class-settings-service.php with:
- All option constants defined
- All getter methods implemented
- is_ip_whitelisted() method with CIDR support
- sanitize_ip_whitelist() method for Settings API
  </verify>
  <done>SettingsService class created with IP validation and settings getters</done>
</task>

<task type="auto">
  <name>Task 2: Create Settings_Page with WordPress Settings API</name>
  <files>includes/admin/class-settings-page.php, includes/class-plugin.php</files>
  <action>
Create Settings_Page class in TSM\Admin namespace:

1. **Constructor**:
   - add_action('admin_menu', array($this, 'add_settings_page'))
   - add_action('admin_init', array($this, 'register_settings'))

2. **add_settings_page()** method:
   - Use add_submenu_page()
   - Parent: 'test-script-manager' (main plugin menu)
   - Page title: 'Settings'
   - Menu title: 'Settings'
   - Capability: 'manage_options'
   - Slug: 'test-script-manager-settings'
   - Callback: array($this, 'render_page')

3. **register_settings()** method:
   - **General tab settings**:
     - register_setting('tsm_general', 'tsm_execution_timeout', sanitize callback clamps to 1-300)
     - register_setting('tsm_general', 'tsm_output_limit', sanitize to positive int)
     - add_settings_section('tsm_general_section', 'Execution Settings', null, 'tsm_general')
     - add_settings_field for timeout (number input, min=1, max=300)
     - add_settings_field for output_limit (number input with MB conversion)

   - **Security tab settings**:
     - register_setting('tsm_security', 'tsm_ip_whitelist_enabled', sanitize to bool)
     - register_setting('tsm_security', 'tsm_ip_whitelist', array(SettingsService, 'sanitize_ip_whitelist'))
     - add_settings_section('tsm_security_section', 'IP Whitelist', null, 'tsm_security')
     - add_settings_field for enabled checkbox
     - add_settings_field for whitelist textarea

4. **render_page()** method:
   - Get active tab from $_GET['tab'], default 'general'
   - Render wrap div with h1 "Test Script Manager Settings"
   - Render nav-tab-wrapper with two tabs: General, Security
   - Render form with action="options.php"
   - Call settings_fields() with 'tsm_' . $active_tab
   - Call do_settings_sections() with 'tsm_' . $active_tab
   - Call submit_button()

5. **Field render methods**:
   - render_timeout_field(): number input, current value from get_option
   - render_output_limit_field(): number input (in MB), convert from bytes
   - render_ip_enabled_field(): checkbox input
   - render_ip_whitelist_field(): textarea, rows=10, cols=50, placeholder="One IP per line, supports CIDR (e.g., 192.168.1.0/24)"

6. **Update includes/class-plugin.php**:
   - Add require_once for class-settings-page.php in load_dependencies()
   - Instantiate Settings_Page in register_hooks() within is_admin() block

Include proper namespace, class docblock, PHPDoc for all methods.
  </action>
  <verify>
1. File exists at includes/admin/class-settings-page.php
2. class-plugin.php loads and initializes Settings_Page
3. Navigate to Test Script Manager > Settings in WP admin
4. Two tabs visible: General, Security
5. General tab: timeout and output limit fields
6. Security tab: IP whitelist enabled checkbox and textarea
7. Save settings and reload - values persist
  </verify>
  <done>Settings_Page created with tabs, all fields render and save correctly</done>
</task>

<task type="auto">
  <name>Task 3: Integrate IP whitelist and timeout into ExecutionService</name>
  <files>includes/services/class-execution-service.php</files>
  <action>
Update ExecutionService to check IP whitelist before execution and use configurable timeout:

1. **Modify execute() method** - add IP check at the beginning, right after the initial reset:
   ```php
   // Check IP whitelist (Phase 7, Plan 02)
   if ( ! SettingsService::is_ip_whitelisted() ) {
       return new \WP_Error(
           'tsm_ip_not_whitelisted',
           __( 'Your IP address is not whitelisted for script execution.', 'test-script-manager' )
       );
   }
   ```

2. **Update timeout handling** - use SettingsService::get_timeout() as default:
   - Change DEFAULT_TIMEOUT constant usage to get from settings
   - If $timeout parameter is not provided (or uses default), get from SettingsService
   - Example: `$timeout = $timeout ?? SettingsService::get_timeout();`
   - Keep the 1-300 clamping logic

3. **Update output limit** - use SettingsService::get_output_limit():
   - Replace MAX_OUTPUT_SIZE constant with SettingsService::get_output_limit() call
   - Keep truncation logic but use dynamic limit

4. **Add use statement** at top of file:
   ```php
   use TSM\Services\SettingsService;
   ```

Note: The constants (MAX_OUTPUT_SIZE, MAX_TIMEOUT, DEFAULT_TIMEOUT) should remain as fallback defaults if settings aren't available.
  </action>
  <verify>
1. Grep class-execution-service.php for "SettingsService::is_ip_whitelisted"
2. Grep class-execution-service.php for "SettingsService::get_timeout"
3. Grep class-execution-service.php for "use TSM\\Services\\SettingsService"
4. Verify the integration by checking file content for all three patterns
  </verify>
  <done>ExecutionService checks IP whitelist and uses configurable timeout/output limits</done>
</task>

</tasks>

<verification>
1. Navigate to Test Script Manager > Settings
2. Verify General tab has:
   - Execution Timeout field (1-300 seconds)
   - Output Limit field (in MB)
3. Verify Security tab has:
   - Enable IP Whitelist checkbox
   - IP Whitelist textarea with placeholder
4. Change timeout to 60 seconds, save, reload - value persists
5. Enable IP whitelist, add "127.0.0.1", save, reload - values persist
6. Grep ExecutionService for SettingsService::get_timeout() call
7. Grep ExecutionService for SettingsService::is_ip_whitelisted() call
</verification>

<success_criteria>
- Settings page accessible from Test Script Manager submenu
- All settings save and persist correctly
- IP whitelist blocks execution when enabled and IP not in list
- IP whitelist supports both single IPs and CIDR notation
- Execution timeout is configurable and enforced
</success_criteria>

<output>
After completion, create `.planning/phases/07-output-enhancements-polish/07-02-SUMMARY.md`
</output>
