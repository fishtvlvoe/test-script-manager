---
phase: 07-output-enhancements-polish
plan: 04
type: execute
wave: 2
depends_on: ["07-03"]
files_modified:
  - includes/services/class-script-service.php
  - includes/admin/views/main-page.php
autonomous: true

must_haves:
  truths:
    - "User can filter scripts by category in the script list"
    - "User can assign categories to scripts when editing"
    - "User can create a new script from a template"
    - "Template library shows common script patterns with descriptions"
    - "Deleting a script removes its category associations"
  artifacts:
    - path: "includes/services/class-script-service.php"
      provides: "Script deletion with category cleanup"
      contains: "delete_script_categories"
    - path: "includes/admin/views/main-page.php"
      provides: "UI for category filter, category assignment, and template selection"
      contains: "tsm-category-filter"
  key_links:
    - from: "includes/services/class-script-service.php"
      to: "includes/services/class-category-service.php"
      via: "CategoryService::delete_script_categories() in delete()"
      pattern: "delete_script_categories"
    - from: "includes/admin/views/main-page.php"
      to: "/test-script-manager/v1/categories"
      via: "AJAX fetch for category dropdown"
      pattern: "categories"
    - from: "includes/admin/views/main-page.php"
      to: "/test-script-manager/v1/templates"
      via: "AJAX fetch for template list in modal"
      pattern: "templates"
---

<objective>
Integrate categories and templates into the main admin UI.

Purpose: With the backend services and APIs ready from Plan 03, this plan adds the user-facing UI elements: category filter dropdown, category assignment in edit mode, and template selection modal.

Output: Updated ScriptService with category cleanup, and main-page.php with category filter, category selector, and template modal.
</objective>

<execution_context>
@/Users/fishtv/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fishtv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-output-enhancements-polish/07-RESEARCH.md
@.planning/phases/07-output-enhancements-polish/07-03-SUMMARY.md

# Relevant source files
@includes/services/class-script-service.php
@includes/services/class-category-service.php
@includes/admin/views/main-page.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update ScriptService to clean up categories on delete</name>
  <files>includes/services/class-script-service.php</files>
  <action>
Update ScriptService::delete() to clean up category associations:

1. **Add use statement** at top of file (if not already present):
   ```php
   use TSM\Services\CategoryService;
   ```

2. **Modify delete() method** - add category cleanup after the VersionService::delete_all_versions() call:
   ```php
   // Delete category associations (Phase 7, Plan 04)
   CategoryService::delete_script_categories($id);
   ```

This ensures that when a script is deleted, all its category associations in tsm_script_tags table are also removed, preventing orphaned records.
  </action>
  <verify>
1. Grep class-script-service.php for "CategoryService::delete_script_categories"
2. Grep class-script-service.php for "use TSM\\Services\\CategoryService" or verify CategoryService is used
  </verify>
  <done>ScriptService::delete() now removes category associations before deleting script</done>
</task>

<task type="auto">
  <name>Task 2: Add category filter and assignment UI</name>
  <files>includes/admin/views/main-page.php</files>
  <action>
Update main-page.php to add category features:

1. **Add category filter dropdown** in the script list header area:
   - Container div with class "tsm-category-filter"
   - Select dropdown with id "tsm-category-select"
   - Default option: "All Categories"
   - Populated via AJAX from /categories endpoint on page load
   - On change, filter displayed scripts (client-side filter using data-category attribute on script items)

2. **Add category selector in script edit form**:
   - Label: "Categories"
   - Multi-select dropdown or checkbox list with class "tsm-category-selector"
   - Load current script's categories when editing (GET /scripts/{id}/categories)
   - Update categories on script save (PUT /scripts/{id}/categories after main save)

3. **Add data-category attribute to script list items**:
   - Each script row gets data-category="[comma-separated category IDs]"
   - Client-side filter shows/hides rows based on selected category

4. **JavaScript additions**:
   - fetchCategories() - GET /categories, populate dropdown
   - filterByCategory(categoryId) - show/hide scripts based on data-category
   - loadScriptCategories(scriptId) - GET /scripts/{id}/categories, populate selector
   - saveScriptCategories(scriptId, categoryIds) - PUT /scripts/{id}/categories

5. **CSS additions**:
   - .tsm-category-filter { margin-bottom: 10px; display: flex; align-items: center; gap: 10px; }
   - .tsm-category-filter select { min-width: 200px; }
   - .tsm-category-selector { margin: 10px 0; }
  </action>
  <verify>
1. Grep main-page.php for "tsm-category-filter"
2. Grep main-page.php for "tsm-category-selector"
3. Grep main-page.php for "fetchCategories"
4. Grep main-page.php for "/categories"
  </verify>
  <done>Category filter dropdown and category assignment UI added to main page</done>
</task>

<task type="auto">
  <name>Task 3: Add template selection modal</name>
  <files>includes/admin/views/main-page.php</files>
  <action>
Add "New from Template" functionality:

1. **Add "New from Template" button** next to existing "New Script" button:
   - Button with class "button tsm-new-from-template"
   - Icon: dashicons-welcome-add-page
   - Text: "New from Template"

2. **Add template selection modal HTML**:
   - Modal overlay with class "tsm-template-modal-overlay"
   - Modal container with class "tsm-template-modal"
   - Header: "Select Template"
   - Template list container with class "tsm-template-list"
   - Close button

3. **Template item structure**:
   - Each template: div with class "tsm-template-item" and data-template-id
   - Template name (h4)
   - Template description (p)
   - "Use This Template" button

4. **JavaScript additions**:
   - openTemplateModal() - show modal, fetch templates if not cached
   - fetchTemplates() - GET /templates, populate modal
   - closeTemplateModal() - hide modal
   - createFromTemplate(templateId) - prompt for name, POST /templates/{id}/create, then load new script in editor

5. **CSS additions**:
   - .tsm-template-modal-overlay { position: fixed; inset: 0; background: rgba(0,0,0,0.5); z-index: 100000; display: none; }
   - .tsm-template-modal-overlay.active { display: flex; justify-content: center; align-items: center; }
   - .tsm-template-modal { background: white; max-width: 600px; width: 90%; max-height: 80vh; overflow-y: auto; border-radius: 4px; padding: 20px; }
   - .tsm-template-item { padding: 15px; border-bottom: 1px solid #ddd; cursor: pointer; }
   - .tsm-template-item:hover { background: #f9f9f9; }
   - .tsm-template-item h4 { margin: 0 0 5px 0; }
   - .tsm-template-item p { margin: 0 0 10px 0; color: #666; }

6. **Flow**:
   - User clicks "New from Template"
   - Modal opens with template list
   - User clicks template's "Use" button
   - Prompt asks for script name
   - Script created via API
   - Modal closes, new script loaded in editor
  </action>
  <verify>
1. Grep main-page.php for "tsm-new-from-template"
2. Grep main-page.php for "tsm-template-modal"
3. Grep main-page.php for "fetchTemplates"
4. Grep main-page.php for "/templates"
  </verify>
  <done>Template selection modal and "New from Template" button added to main page</done>
</task>

</tasks>

<verification>
1. Check ScriptService::delete() calls CategoryService::delete_script_categories()
2. Check main-page.php has category filter UI elements
3. Check main-page.php has category assignment UI elements
4. Check main-page.php has template modal UI elements
5. Verify JavaScript functions exist: fetchCategories, filterByCategory, fetchTemplates, createFromTemplate
</verification>

<success_criteria>
- User can filter scripts by category in the script list
- User can assign categories to scripts when editing
- User can create a new script from a template
- Template library displays 6 common script patterns
- Deleting a script removes its category associations
</success_criteria>

<output>
After completion, create `.planning/phases/07-output-enhancements-polish/07-04-SUMMARY.md`
</output>
