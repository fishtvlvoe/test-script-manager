---
phase: 02-script-crud-storage
plan: 03
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - includes/admin/class-admin-page.php
  - assets/css/admin-page.css
  - assets/js/admin-page.js
autonomous: false

must_haves:
  truths:
    - "Admin page shows script list table with name, language, created date, last executed date"
    - "User can click 'New Script' button to show create form"
    - "User can enter script name and slug in the form"
    - "User can click 'Save' to create script (calls REST API)"
    - "User can see success/error message after saving"
    - "User can search scripts by name"
  artifacts:
    - path: "includes/admin/class-admin-page.php"
      provides: "Admin page with script list UI"
      contains: "render_page(), enqueue_scripts()"
      min_lines: 150
    - path: "assets/css/admin-page.css"
      provides: "Admin page styling"
      min_lines: 80
    - path: "assets/js/admin-page.js"
      provides: "Admin page JavaScript (AJAX, form handling)"
      min_lines: 100
  key_links:
    - from: "assets/js/admin-page.js"
      to: "/wp-json/test-script-manager/v1/scripts"
      via: "fetch() API calls"
      pattern: "fetch.*test-script-manager/v1/scripts"
    - from: "includes/admin/class-admin-page.php"
      to: "assets/css/admin-page.css"
      via: "wp_enqueue_style()"
      pattern: "wp_enqueue_style.*admin-page"
    - from: "includes/admin/class-admin-page.php"
      to: "assets/js/admin-page.js"
      via: "wp_enqueue_script()"
      pattern: "wp_enqueue_script.*admin-page"
---

<objective>
建立基本的腳本管理後台介面,顯示腳本列表和建立表單

Purpose: 提供使用者友善的後台介面,讓管理員可以檢視和建立腳本
Output: 更新的 Admin_Page 類別和前端資源 (CSS + JS)
</objective>

<execution_context>
@/Users/fishtv/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fishtv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-script-crud-storage/02-01-PLAN.md
@.planning/phases/02-script-crud-storage/02-02-PLAN.md
@includes/admin/class-admin-page.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Admin_Page with script list UI and asset enqueuing</name>
  <files>includes/admin/class-admin-page.php</files>
  <action>
更新 Admin_Page 類別,加入腳本列表 UI 和資源載入:

**新增方法:**

1. **enqueue_scripts()** - 載入 CSS 和 JS
   - Hook: `admin_enqueue_scripts`
   - 只在 test-script-manager 頁面載入 (檢查 $_GET['page'])

   ```php
   public function enqueue_scripts( $hook ) {
       // Only load on our admin page.
       if ( 'toplevel_page_test-script-manager' !== $hook ) {
           return;
       }

       // Enqueue CSS.
       wp_enqueue_style(
           'tsm-admin-page',
           TSM_PLUGIN_URL . 'assets/css/admin-page.css',
           array(),
           '1.0.0'
       );

       // Enqueue JS.
       wp_enqueue_script(
           'tsm-admin-page',
           TSM_PLUGIN_URL . 'assets/js/admin-page.js',
           array( 'jquery' ),
           '1.0.0',
           true
       );

       // Localize script with REST API data.
       wp_localize_script(
           'tsm-admin-page',
           'tsmAdmin',
           array(
               'restUrl'   => rest_url( 'test-script-manager/v1' ),
               'nonce'     => wp_create_nonce( 'wp_rest' ),
               'ajaxUrl'   => admin_url( 'admin-ajax.php' ),
           )
       );
   }
   ```

2. **更新 render_page()** - 改寫頁面 HTML

   ```php
   public function render_page() {
       // Permission check (defense in depth).
       if ( ! current_user_can( 'manage_options' ) ) {
           wp_die( esc_html__( 'You do not have permission to access this page.', 'test-script-manager' ) );
       }
       ?>
       <div class="wrap tsm-admin-page">
           <h1>
               <?php esc_html_e( 'Test Script Manager', 'test-script-manager' ); ?>
               <button type="button" class="page-title-action" id="tsm-new-script">
                   <?php esc_html_e( 'New Script', 'test-script-manager' ); ?>
               </button>
           </h1>

           <div class="tsm-container">
               <!-- Sidebar -->
               <div class="tsm-sidebar">
                   <!-- Search box -->
                   <div class="tsm-search-box">
                       <input type="text" id="tsm-search" placeholder="<?php esc_attr_e( 'Search scripts...', 'test-script-manager' ); ?>">
                   </div>

                   <!-- Script list -->
                   <div class="tsm-script-list" id="tsm-script-list">
                       <div class="tsm-loading"><?php esc_html_e( 'Loading scripts...', 'test-script-manager' ); ?></div>
                   </div>
               </div>

               <!-- Main content -->
               <div class="tsm-main-content">
                   <!-- Create form (hidden by default) -->
                   <div class="tsm-create-form" id="tsm-create-form" style="display: none;">
                       <h2><?php esc_html_e( 'Create New Script', 'test-script-manager' ); ?></h2>

                       <div class="tsm-form-group">
                           <label for="tsm-script-name"><?php esc_html_e( 'Script Name', 'test-script-manager' ); ?></label>
                           <input type="text" id="tsm-script-name" required>
                       </div>

                       <div class="tsm-form-group">
                           <label for="tsm-script-slug"><?php esc_html_e( 'Slug', 'test-script-manager' ); ?></label>
                           <input type="text" id="tsm-script-slug" required>
                           <p class="description"><?php esc_html_e( 'Lowercase letters, numbers, and hyphens only', 'test-script-manager' ); ?></p>
                       </div>

                       <div class="tsm-form-group">
                           <label for="tsm-script-code"><?php esc_html_e( 'Code', 'test-script-manager' ); ?></label>
                           <textarea id="tsm-script-code" rows="10" required></textarea>
                       </div>

                       <div class="tsm-form-actions">
                           <button type="button" class="button button-primary" id="tsm-save-script">
                               <?php esc_html_e( 'Save Script', 'test-script-manager' ); ?>
                           </button>
                           <button type="button" class="button" id="tsm-cancel-create">
                               <?php esc_html_e( 'Cancel', 'test-script-manager' ); ?>
                           </button>
                       </div>

                       <div class="tsm-message" id="tsm-message" style="display: none;"></div>
                   </div>

                   <!-- Welcome message (shown by default) -->
                   <div class="tsm-welcome" id="tsm-welcome">
                       <h2><?php esc_html_e( 'Welcome to Test Script Manager', 'test-script-manager' ); ?></h2>
                       <p><?php esc_html_e( 'Select a script from the sidebar or create a new one.', 'test-script-manager' ); ?></p>
                   </div>
               </div>
           </div>
       </div>
       <?php
   }
   ```

3. **在 register_hooks() 方法中註冊 enqueue hook:**
   ```php
   public function register_hooks() {
       add_action( 'admin_menu', array( $this, 'register_menu' ) );
       add_action( 'admin_enqueue_scripts', array( $this, 'enqueue_scripts' ) );
   }
   ```

**移除舊的 debug 資訊** - 不再需要顯示 REST URL 和 nonce

**命名空間:** 保持 `namespace TSM\Admin;`
  </action>
  <verify>
PHP 語法檢查: `php -l includes/admin/class-admin-page.php`
確認包含 enqueue_scripts() 方法
確認 render_page() 包含 tsm-script-list 和 tsm-create-form
  </verify>
  <done>
Admin_Page 類別更新完成,包含 UI 結構和資源載入
  </done>
</task>

<task type="auto">
  <name>Task 2: Create CSS and JavaScript for admin interface</name>
  <files>
assets/css/admin-page.css
assets/js/admin-page.js
  </files>
  <action>
建立前端資源:

**assets/css/admin-page.css:**

```css
/* Container layout */
.tsm-admin-page .tsm-container {
    display: flex;
    gap: 20px;
    margin-top: 20px;
}

/* Sidebar */
.tsm-sidebar {
    flex: 0 0 300px;
    background: #fff;
    border: 1px solid #ccd0d4;
    box-shadow: 0 1px 1px rgba(0,0,0,.04);
}

.tsm-search-box {
    padding: 15px;
    border-bottom: 1px solid #ddd;
}

.tsm-search-box input {
    width: 100%;
}

.tsm-script-list {
    max-height: 600px;
    overflow-y: auto;
}

.tsm-script-item {
    padding: 12px 15px;
    border-bottom: 1px solid #f0f0f1;
    cursor: pointer;
    transition: background 0.2s;
}

.tsm-script-item:hover {
    background: #f6f7f7;
}

.tsm-script-item.active {
    background: #f0f0f1;
}

.tsm-script-name {
    font-weight: 600;
    margin-bottom: 5px;
}

.tsm-script-meta {
    font-size: 12px;
    color: #646970;
}

.tsm-loading {
    padding: 20px;
    text-align: center;
    color: #646970;
}

/* Main content */
.tsm-main-content {
    flex: 1;
    background: #fff;
    border: 1px solid #ccd0d4;
    box-shadow: 0 1px 1px rgba(0,0,0,.04);
    padding: 20px;
}

/* Form */
.tsm-form-group {
    margin-bottom: 20px;
}

.tsm-form-group label {
    display: block;
    margin-bottom: 5px;
    font-weight: 600;
}

.tsm-form-group input[type="text"],
.tsm-form-group textarea {
    width: 100%;
    max-width: 600px;
}

.tsm-form-group .description {
    margin-top: 5px;
    color: #646970;
}

.tsm-form-actions {
    margin-top: 20px;
}

.tsm-message {
    margin-top: 15px;
    padding: 10px 15px;
    border-left: 4px solid;
}

.tsm-message.success {
    background: #ecf7ed;
    border-color: #46b450;
    color: #1e4620;
}

.tsm-message.error {
    background: #fcf0f1;
    border-color: #dc3232;
    color: #761919;
}

/* Welcome message */
.tsm-welcome {
    text-align: center;
    padding: 60px 20px;
    color: #646970;
}
```

**assets/js/admin-page.js:**

```javascript
(function($) {
    'use strict';

    let scripts = [];

    // Load scripts on page load
    $(document).ready(function() {
        loadScripts();
        initEventHandlers();
    });

    // Initialize event handlers
    function initEventHandlers() {
        // New script button
        $('#tsm-new-script').on('click', showCreateForm);

        // Cancel create
        $('#tsm-cancel-create').on('click', hideCreateForm);

        // Save script
        $('#tsm-save-script').on('click', saveScript);

        // Search
        $('#tsm-search').on('keyup', debounce(searchScripts, 300));

        // Auto-generate slug from name
        $('#tsm-script-name').on('keyup', function() {
            const name = $(this).val();
            const slug = name.toLowerCase()
                .replace(/[^a-z0-9]+/g, '-')
                .replace(/^-+|-+$/g, '');
            $('#tsm-script-slug').val(slug);
        });
    }

    // Load scripts from REST API
    function loadScripts() {
        $.ajax({
            url: tsmAdmin.restUrl + '/scripts',
            method: 'GET',
            beforeSend: function(xhr) {
                xhr.setRequestHeader('X-WP-Nonce', tsmAdmin.nonce);
            },
            success: function(response) {
                scripts = response.scripts || [];
                renderScriptList(scripts);
            },
            error: function() {
                $('#tsm-script-list').html('<div class="tsm-loading">Failed to load scripts</div>');
            }
        });
    }

    // Render script list
    function renderScriptList(scriptList) {
        const $list = $('#tsm-script-list');

        if (scriptList.length === 0) {
            $list.html('<div class="tsm-loading">No scripts yet. Create one to get started!</div>');
            return;
        }

        let html = '';
        scriptList.forEach(script => {
            const lastExecuted = script.last_executed_at
                ? new Date(script.last_executed_at).toLocaleDateString()
                : 'Never';

            html += `
                <div class="tsm-script-item" data-id="${script.id}">
                    <div class="tsm-script-name">${escapeHtml(script.name)}</div>
                    <div class="tsm-script-meta">
                        ${script.language} • Last run: ${lastExecuted}
                    </div>
                </div>
            `;
        });

        $list.html(html);
    }

    // Show create form
    function showCreateForm() {
        $('#tsm-welcome').hide();
        $('#tsm-create-form').show();
        $('#tsm-script-name').focus();
    }

    // Hide create form
    function hideCreateForm() {
        $('#tsm-create-form').hide();
        $('#tsm-welcome').show();
        clearForm();
    }

    // Save script
    function saveScript() {
        const name = $('#tsm-script-name').val().trim();
        const slug = $('#tsm-script-slug').val().trim();
        const code = $('#tsm-script-code').val().trim();

        if (!name || !slug || !code) {
            showMessage('Please fill in all required fields.', 'error');
            return;
        }

        $.ajax({
            url: tsmAdmin.restUrl + '/scripts',
            method: 'POST',
            beforeSend: function(xhr) {
                xhr.setRequestHeader('X-WP-Nonce', tsmAdmin.nonce);
            },
            data: {
                name: name,
                slug: slug,
                code: code,
                language: 'php'
            },
            success: function(response) {
                showMessage('Script created successfully!', 'success');
                clearForm();
                loadScripts();

                // Hide form after 1 second
                setTimeout(hideCreateForm, 1000);
            },
            error: function(xhr) {
                const message = xhr.responseJSON?.error || 'Failed to create script';
                showMessage(message, 'error');
            }
        });
    }

    // Search scripts
    function searchScripts() {
        const keyword = $('#tsm-search').val().trim();

        if (!keyword) {
            renderScriptList(scripts);
            return;
        }

        const filtered = scripts.filter(script => {
            return script.name.toLowerCase().includes(keyword.toLowerCase()) ||
                   script.code.toLowerCase().includes(keyword.toLowerCase());
        });

        renderScriptList(filtered);
    }

    // Show message
    function showMessage(text, type) {
        const $message = $('#tsm-message');
        $message
            .removeClass('success error')
            .addClass(type)
            .text(text)
            .show();
    }

    // Clear form
    function clearForm() {
        $('#tsm-script-name, #tsm-script-slug, #tsm-script-code').val('');
        $('#tsm-message').hide();
    }

    // Utility: debounce
    function debounce(func, wait) {
        let timeout;
        return function() {
            const context = this;
            const args = arguments;
            clearTimeout(timeout);
            timeout = setTimeout(() => func.apply(context, args), wait);
        };
    }

    // Utility: escape HTML
    function escapeHtml(text) {
        const map = {
            '&': '&amp;',
            '<': '&lt;',
            '>': '&gt;',
            '"': '&quot;',
            "'": '&#039;'
        };
        return text.replace(/[&<>"']/g, m => map[m]);
    }

})(jQuery);
```

**建立目錄:**
```bash
mkdir -p assets/css assets/js
```
  </action>
  <verify>
檢查檔案存在:
ls -la assets/css/admin-page.css
ls -la assets/js/admin-page.js

JavaScript 語法檢查 (如果有 node):
node -c assets/js/admin-page.js
  </verify>
  <done>
CSS 和 JavaScript 檔案建立完成,包含完整的列表、搜尋、建立功能
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>完整的腳本管理後台介面,包含列表、搜尋和建立功能</what-built>
  <how-to-verify>
**測試步驟:**

1. **訪問後台頁面**
   - 前往: https://test.buygo.me/wp-admin/admin.php?page=test-script-manager
   - 確認頁面載入正常,沒有 JavaScript 錯誤 (開啟瀏覽器 Console)

2. **測試建立腳本**
   - 點擊 "New Script" 按鈕
   - 輸入 Name: "Test Script 1"
   - Slug 應該自動填入: "test-script-1"
   - 輸入 Code:
     ```php
     echo "Hello from Test Script 1";
     ```
   - 點擊 "Save Script"
   - 確認出現成功訊息
   - 確認左側列表出現新腳本

3. **測試搜尋功能**
   - 在搜尋框輸入 "Test"
   - 確認列表過濾顯示符合的腳本
   - 清空搜尋框
   - 確認列表恢復完整

4. **測試檔案系統**
   - 前往: https://test.buygo.me/test-scripts/test-test-script-1.php
   - 確認檔案可訪問,看到 "Hello from Test Script 1"

5. **測試權限**
   - 登出 WordPress
   - 嘗試訪問: https://test.buygo.me/wp-json/test-script-manager/v1/scripts
   - 確認返回 401 或 403 錯誤

**預期結果:**
- ✅ 後台頁面正常載入,無 JavaScript 錯誤
- ✅ 可以建立新腳本,資料同步到資料庫和檔案系統
- ✅ 搜尋功能正常
- ✅ 腳本檔案可透過瀏覽器直接訪問
- ✅ 未登入無法訪問 REST API
  </how-to-verify>
  <resume-signal>回報測試結果,描述任何問題或輸入 "approved" 繼續</resume-signal>
</task>

</tasks>

<verification>
**整體驗證:**

1. **頁面載入檢查:**
   - 訪問 https://test.buygo.me/wp-admin/admin.php?page=test-script-manager
   - 開啟 Chrome DevTools Console
   - 確認沒有 JavaScript 錯誤
   - 確認 tsmAdmin 物件存在: `console.log(tsmAdmin)`

2. **REST API 呼叫檢查:**
   - 在 Chrome DevTools Network tab
   - 重新載入頁面
   - 確認看到 GET /wp-json/test-script-manager/v1/scripts 請求
   - 確認返回 200 狀態碼

3. **資源載入檢查:**
   - 在 Network tab 確認載入:
     - admin-page.css
     - admin-page.js
   - 確認沒有 404 錯誤
</verification>

<success_criteria>
1. 後台頁面顯示腳本列表 (初次為空)
2. 點擊 "New Script" 顯示建立表單
3. 可以建立新腳本,成功後列表更新
4. 搜尋功能可以過濾腳本
5. 腳本檔案可透過瀏覽器直接訪問執行
6. CSS 和 JavaScript 正確載入,無錯誤
</success_criteria>

<output>
After completion, create `.planning/phases/02-script-crud-storage/02-03-SUMMARY.md`
</output>
